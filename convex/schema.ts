import { defineSchema } from "convex/server";

/**
 * Glimt data model — add tables here as you build the MVP.
 * Example: glimts (image + caption), friendships, widget rotation state.
 */
export default defineSchema({});
